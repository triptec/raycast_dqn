pub fn main() {
    //Create environment
    //Create agent
    //Add agent to environment
    //Init state
    //Create replay buffer

    /*
    while episode < max_episodes do
        action is random value in actionspace
        (state, reward, done) = step(action)
        agent memorize state,reward
        if done
            save in replay
            reset agent

    end
     */
}
